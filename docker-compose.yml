version: '3.8'

services:
  ip-scanner:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ip-scanner
    network_mode: "host"
    privileged: true
    restart: always